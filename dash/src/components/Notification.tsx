"use client";

import { motion } from "framer-motion";

type Props = {};

export default function Notification({}: Props) {
	return (
		<motion.div>
			<p></p>
			<button>x</button>
		</motion.div>
	);
}
