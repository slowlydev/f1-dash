import type { Viewport } from "next";

export const viewport: Viewport = {
	colorScheme: "dark",
	themeColor: "#000000",
	initialScale: 1,
	maximumScale: 10,
	minimumScale: 0.1,
	interactiveWidget: "overlays-content",
	userScalable: true,
};
