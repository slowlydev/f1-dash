import type { Viewport } from "next";

export const viewport: Viewport = {
	colorScheme: "dark",
	themeColor: "#09090b",
	initialScale: 1,
	maximumScale: 10,
	minimumScale: 0.1,
	userScalable: true,
};
